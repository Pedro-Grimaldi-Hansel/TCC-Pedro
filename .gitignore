# ── Ambiente Python ───────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# ── Editor ────────────────────────────────────────────────────────────
.vscode/

# ── Dados (baixados do FTP do DATASUS ou gerados pelo pipeline) ────────
# Pesados e reconstruíveis: a etapa 01 rebaixa/regenera tudo.
dados/
dados_sim/
*.dbc
*.dbf

# ── Artefatos gerados (reproduzíveis ao rodar o pipeline) ─────────────
figuras/
resultados/
features_cache.pkl
*.pkl
*.zip

# ── Instruções locais de ferramenta (não fazem parte do projeto) ──────
CLAUDE.md
